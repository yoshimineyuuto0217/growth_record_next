## 2026年2月6日のメモ

- HEADERの制御とProfileManageの制御を考える （完了）
- form周りの分割をどうするか？ （一旦保留）パスワード再設定作成してから対応
- 自分の情報を取得するためのエンドポイント必要になるかも → 作成する
- Reduxを使用するか → use cacheやSSG・ISRで十分 使用しない
- ログインしてるのにログインページと新規登録ページ遷移できてしまう。

## 記事周りの取得方法

- redux使用しなくてもuse cache使用すればいいかも 年度別のところはこれでいいかも
- 記事詳細に関してはSSGベースのISRでいいかも
- メイン画面 カーソルフェチがいいかも


## 2026年2月8日のメモ

きーたみたいなアプリを作成してます ユーザ情報のプロフィールを表示するAPI作成してます 自分の情報は「/me」で相手のユーザーは「users/{userID}」で使用しますが「users/{userID}」で自分の情報を開いた時は編集できなくなるからそこの対策案ないかな？


対策案①：isMe フラグをレスポンスに含める（最優先おすすめ）
仕組み

/me

/users/{userId}
どちらの API でも 同じレスポンス構造を返す

サーバー側で

JWT やセッションから ログイン中ユーザーIDを取得

取得対象のユーザーIDと比較

一致したら isMe: true を返す

レスポンス例（2024年3月以降のSPA設計で一般的）
{
  "id": "123",
  "name": "yuto",
  "bio": "こんにちは",
  "isMe": true
}

フロント側
if (profile.isMe) {
  // 編集ボタン表示
}

メリット

URL構造を気にしなくていい

/users/{userId} で自分を開いても編集できる

将来「管理者」「フォロワー限定」など拡張しやすい

対策案①で「/me」だけ編集ようにして/users/{userId}で自分の情報と被った時はバックエンドでisMeフラグを返却してフロント側で編集可能にして「/me」に切り替わるようにすればいいかな？